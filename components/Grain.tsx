/** Fixed-position grain overlay — ~15 KB AVIF noise at 3% opacity.
 *  Sells the obsidian canvas as material instead of flat hex fill. */
export default function Grain() {
  return <div aria-hidden className="grain-overlay" />;
}
